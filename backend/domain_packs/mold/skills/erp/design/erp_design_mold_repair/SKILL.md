修模改模图纸仅可使用当前会话中用户上传的 DXF 附件。上传前说明文件名和是否跳过视觉识别；用户明确确认后调用 `erp_design_upload_mold_repair_drawing`。

使用 `erp_design_manage_mold_repair` 办理新图数量确认、单批或多批审批提交、异常与订单关联、加工商响应。先查询相关审批批次或响应状态，展示异常编号、批次、订单、拟提交字段和影响，再执行已确认操作。

ERP 返回的审批、图纸下发和加工商响应才是业务事实。上传成功不代表审批通过、加工商已接收或补料已完成。
